---
interface Props {
    href: string;
}
const {href} = Astro.props

---

<li>
    <a 
      class="flex gap-4 text-zinc-300 hover:text-zinc-100 py-2 font-medium transition duration-300"
      href={href}
      aria-label="link">
        <slot/>
    </a>
</li>